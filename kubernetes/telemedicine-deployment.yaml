apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemedicine-service
  namespace: smartmedicare
  labels:
    app: telemedicine-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: telemedicine-service
  template:
    metadata:
      labels:
        app: telemedicine-service
        tier: backend
    spec:
      containers:
      - name: telemedicine-service
        image: smartmedicare/telemedicine-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5007
          name: http
        env:
        - name: PORT
          value: "5007"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: MONGO_URI
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5007
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5007
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  namespace: smartmedicare
  labels:
    app: telemedicine-service
spec:
  selector:
    app: telemedicine-service
  ports:
  - port: 5007
    targetPort: 5007
    protocol: TCP
    name: http
  type: ClusterIP
