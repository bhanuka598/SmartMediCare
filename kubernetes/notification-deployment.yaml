apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: smartmedicare
  labels:
    app: notification-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        tier: backend
    spec:
      containers:
      - name: notification-service
        image: smartmedicare/notification-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5004
          name: http
        env:
        - name: PORT
          value: "5004"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: MONGO_URI
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: smartmedicare
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - port: 5004
    targetPort: 5004
    protocol: TCP
    name: http
  type: ClusterIP
