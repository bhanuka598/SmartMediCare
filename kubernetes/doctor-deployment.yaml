apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  namespace: smartmedicare
  labels:
    app: doctor-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
        tier: backend
    spec:
      containers:
      - name: doctor-service
        image: smartmedicare/doctor-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5003
          name: http
        env:
        - name: PORT
          value: "5003"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: MONGO_URI
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: JWT_SECRET
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: smartmedicare
  labels:
    app: doctor-service
spec:
  selector:
    app: doctor-service
  ports:
  - port: 5003
    targetPort: 5003
    protocol: TCP
    name: http
  type: ClusterIP
