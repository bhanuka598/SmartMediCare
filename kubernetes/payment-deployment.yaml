apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: smartmedicare
  labels:
    app: payment-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        tier: backend
    spec:
      containers:
      - name: payment-service
        image: smartmedicare/payment-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5006
          name: http
        env:
        - name: PORT
          value: "5006"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: MONGO_URI
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: smartmedicare-secrets
              key: JWT_SECRET
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5006
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: smartmedicare
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
  - port: 5006
    targetPort: 5006
    protocol: TCP
    name: http
  type: ClusterIP
